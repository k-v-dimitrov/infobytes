import React from "react"
import { Spinner } from "@gluestack-ui/themed"
import { Screen } from "app/components"

export const Feed = () => {
  return (
    <Screen>
      <Spinner />
    </Screen>
  )
}
